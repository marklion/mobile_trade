{
  "serial": "COM1",
  "baudRate": 9600,
  "script": "function (array) { return array[0] + array[1]; }",
  "_comment": {
    "serial": "串口名，Windows 如 COM1，Linux 如 /dev/ttyUSB0",
    "baudRate": "波特率，默认 9600",
    "script": "解析函数，入参 array 为本次串口收到的字节数组(0~255)，返回数字即为当前重量；返回 null 表示忽略本包"
  }
}
